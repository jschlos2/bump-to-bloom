# BumpToBloom — Parenting Companion Prototype

A single-file HTML/CSS/JS prototype of BumpToBloom, a parenting companion app concept.

## View it live

Once GitHub Pages is enabled on this repo (see below), the prototype will be live at:

`https://<your-username>.github.io/<repo-name>/`

## Run it locally

Just open `index.html` in any browser — no build step, no dependencies.
